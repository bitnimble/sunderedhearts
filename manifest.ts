import { PageManifest } from '../../../config/manifest';

export const manifest: PageManifest = {
  title: "Sundered Hearts NPC Host club",
  head: {
    googleFonts: [{ name: 'Fraunces', weights: [100, 300, 400] }, 'Ms Madi', 'DM Sans'],
  }
};
